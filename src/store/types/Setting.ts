import { Color } from '../../render'
import { Supertile, Weave, Assignment } from '../../components'

type Setting = number | string | Color | Function | Supertile | Weave | Assignment | object

export default Setting
