// import currentIteration from './currentIteration'
// import state from './state'

export default {
	// tileConfig: {
	// 	tileSize: () => state.tileConfig.tileSize * Math.pow(0.5, currentIteration.i)
	// },
	// colorConfig: {
	// 	opacity: () => state.colorConfig.opacity * Math.pow(0.5, currentIteration.i)
	// }
}
