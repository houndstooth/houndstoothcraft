// tslint:disable:no-magic-numbers

import { StripePosition } from './stripe'

const HALF: number = 1 / 2
// tslint:disable-next-line:no-any
const PERIMETER_SCALAR: StripePosition = 2 as any

export {
	HALF,
	PERIMETER_SCALAR,
}
