{
  "name": "web-render",
  "version": "1.0.0",
  "scripts": {
    "start": "webpack-dev-server --hot --inline --content-base dist",
    "deploy": "webpack -p && cf push houndstooth -b https://github.com/cloudfoundry/staticfile-buildpack.git -m 64m",
    "test": "karma start",
    "setup": "npm i -g webpack && npm i && git config user.name \"Douglas Blumeyer\" && git config user.email douglas.blumeyer@gmail.com && git submodule update --init --recursive && git submodule foreach git checkout master && git submodule foreach git config user.name \"Douglas Blumeyer\" && git submodule foreach git config user.email douglas.blumeyer@gmail.com",
    "push-all": "git push && git submodule foreach git push",
    "pull-all": "git pull -r && git submodule foreach git pull -r",
    "lint": "eslint .",
    "lint-fix": "eslint . --fix",
    "ci": "karma start --single-run --browsers ChromeNoSandboxHeadless"
  },
  "dependencies": {
    "file-saver": "^1.3.3"
  },
  "devDependencies": {
    "babel-core": "^6.25.0",
    "babel-loader": "^7.1.0",
    "babel-plugin-rewire": "^1.1.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.1.1",
    "jasmine": "^2.6.0",
    "karma": "^1.7.0",
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^1.1.0",
    "karma-jasmine-html-reporter": "^0.2.2",
    "karma-webpack": "^2.0.3",
    "path": "^0.12.7",
    "webpack": "^3.0.0",
    "webpack-dev-server": "^2.4.5"
  }
}
